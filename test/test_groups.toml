[Core]
versions = ["lts", "1", "pre"]

# Hard-failing 32-bit lane (BFloat16s/NNlib/Tracker may still hit LLVM soft-promote on i686).
["Core 32-bit"]
group = "Core"
versions = ["1"]
os = ["ubuntu-latest"]
arch = "x86"

[Autodiff]
versions = ["lts", "1", "pre"]
in_all = false

[GPU]
versions = ["lts", "1", "pre"]
in_all = false

[Downstream]
versions = ["lts", "1"]
in_all = false

[Reactant]
versions = ["lts", "1"]
in_all = false

[QA]
versions = ["lts", "1"]
